# name: scheduled-scrape
# on:
#   schedule:
#     - cron: '0 9 * * *'  
#   workflow_dispatch:

# jobs:
#   scrape:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
      
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
      
#       - name: Install dependencies
#         run: python -m pip install -r requirements.txt
      
#       - name: Run scraper
#         env:
#           NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
#           DB_COMPANIES_ID: ${{ secrets.DB_COMPANIES_ID }}
#           DB_OFFERS_ID: ${{ secrets.DB_OFFERS_ID }}
#           DB_APPLICATIONS_ID: ${{ secrets.DB_APPLICATIONS_ID }}
#           DB_CONTACTS_ID: ${{ secrets.DB_CONTACTS_ID }}
#           DB_DOCUMENTS_ID: ${{ secrets.DB_DOCUMENTS_ID }}
#           DB_OFFERS_RECEIVED_ID: ${{ secrets.DB_OFFERS_RECEIVED_ID }}
#           DRY_RUN: false
#           LOG_LEVEL: INFO
#         run: python src/main.py